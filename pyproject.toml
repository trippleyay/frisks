[project]
name = "frisks"
version = "0.1.0"
description = "Agent-native crypto options Strategy Hunter (Binance European Options)"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
